spring:
  application:
    name: payment
  session:
    store-type: none
  datasource:
    url: "jdbc:postgresql://payment-postgres:5432/postgres"
    username: "postgres"
    password: ""
    driver-class-name: "org.postgresql.Driver"
    jpa:
      database: "postgresql"
      hibernate:
        ddl-auto: "update"
#  rabbitmq:
#    host: rabbitmq

logging:
  level:
    org: ERROR

eureka:
  instance:
    preferIpAddress: true
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/